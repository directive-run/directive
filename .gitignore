# Dependencies
node_modules/

# Build outputs
dist/
*.tsbuildinfo

# Test coverage
coverage/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Next
.next

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Extracted website (now in directive-run/directive-docs)
website/

# Claude Code
.claude/
# Local Claude Code plugin state — keep marketplace.json public (it's what
# users register via `/plugin marketplace add directive-run/directive` to
# install the @directive-run/claude-plugin via Claude Code's plugin system).
.claude-plugin/*
!.claude-plugin/marketplace.json

# Git worktrees (sz CLI parallel-dev slots)
worktrees/

# Playwright
playwright-report/
test-results/

# Generated knowledge assets
packages/knowledge/api-skeleton.md
packages/knowledge/examples/
packages/claude-plugin/skills/*/api-skeleton.md
packages/claude-plugin/skills/*/examples.md

# Reference materials (third-party code, not for redistribution)
.references/

# Harness run artifacts — transcripts, sidecars, and combined documents.
# `.ai-harness/` is the library default; `runs/` is the command line's.
.ai-harness/
runs/

# Temporary
tmp/
temp/
.vercel

# Internal working notes — never ship in the public repo. These live locally
# (or in a separate private workspace) and should never be `git add`ed here.
docs/IDEAS.md
docs/AE-AUDIT-*.md
docs/SESSION-HANDOFF-*.md
docs/SHIP-RECOVERY*.md
