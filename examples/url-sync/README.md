# URL Sync

A product catalog that syncs filter, sort, and pagination state with the browser URL.

## Features

- Multi-module system (url + products)
- Two-way sync between URL query params and system state
- Search, category filter, sort, and page navigation
- Live URL display showing current query string

## Run

```bash
pnpm install
pnpm dev
```
