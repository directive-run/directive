# Auth Flow

A login/logout flow with token expiration countdown and auto-refresh.

## Features

- Login form with email/password validation
- Token expiration with reactive 1-second countdown
- Auto-refresh constraints when token nears expiry
- Force-expire button for testing edge cases

## Run

```bash
pnpm install
pnpm dev
```
