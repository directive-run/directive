{
  "name": "@directive-run/example-goal-heist",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@directive-run/core": "workspace:*",
    "@directive-run/ai": "workspace:*"
  },
  "devDependencies": {
    "@directive-run/vite-plugin-api-proxy": "workspace:*",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
