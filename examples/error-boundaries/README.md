# Error Boundaries

A resilient API dashboard with configurable failure rates and recovery strategies.

## Features

- Three service cards (users, orders, analytics) with independent failure rates
- Configurable recovery strategies (retry, fallback, ignore, propagate)
- Per-service error display and status indicators
- Event timeline showing error boundary activity

## Run

```bash
pnpm install
pnpm dev
```
