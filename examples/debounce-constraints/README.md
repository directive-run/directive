# Debounce Constraints

A search input with constraint-driven debounce -- the search fires only after the user stops typing.

## Features

- Debounced constraint triggers search after idle period
- Reactive progress bar showing debounce countdown
- Results list with search stats
- Configurable debounce delay via slider

## Run

```bash
pnpm install
pnpm dev
```
