# Feature Flags

A React-based feature flag manager with persistence, preview, and per-flag controls.

## Features

- Toggle feature flags with live preview panel
- Persistence plugin saves flags to localStorage
- Enable/disable individual effects and constraints
- Inspector showing current system state

## Run

```bash
pnpm install
pnpm dev
```
