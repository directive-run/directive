{
  "name": "@directive-run/example-server",
  "version": "1.0.9",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx tsx --watch src/server.ts",
    "start": "npx tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@directive-run/core": "workspace:*",
    "@directive-run/ai": "workspace:*",
    "@directive-run/el": "workspace:*",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "typescript": "^5.3.0",
    "tsx": "^4.0.0"
  }
}
