{
  "name": "@directive-run/example-schema-patterns",
  "version": "1.0.64",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:builders": "npx tsx builders.ts",
    "test:type-assertion": "npx tsx type-assertion.ts",
    "test:zod": "npx tsx zod-schema.ts",
    "test:mixed": "npx tsx mixed.ts",
    "test": "pnpm typecheck && pnpm test:builders && pnpm test:type-assertion && pnpm test:zod && pnpm test:mixed"
  },
  "dependencies": {
    "@directive-run/core": "workspace:*",
    "@directive-run/el": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "tsx": "^4.0.0"
  }
}
