# Pagination

A filterable, sortable item list with infinite scroll powered by constraint-driven page loading.

## Features

- Search, category filter, and sort controls
- Infinite scroll with intersection observer sentinel
- Multi-module system (filters + list)
- Constraint fires page-load when sentinel is visible

## Run

```bash
pnpm install
pnpm dev
```
