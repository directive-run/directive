# Topic Guard

A chat interface with constraint-driven topic filtering that blocks off-topic messages.

## Features

- Allowed/blocked message classification via constraints
- Per-topic configuration for allowed subjects
- Stats panel with allowed count, blocked count, and block rate
- Guardrail log showing each message's evaluation

## Run

```bash
pnpm install
pnpm dev
```
