# WebSocket

A WebSocket connection manager with auto-reconnect, message feed, and configurable retry.

## Features

- Connect/disconnect with status badge and URL display
- Auto-reconnect with reactive countdown timer
- Message feed showing sent and received messages
- Configurable retry delay and max attempts via sliders

## Run

```bash
pnpm install
pnpm dev
```
