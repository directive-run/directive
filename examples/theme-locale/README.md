# Theme & Locale

A preferences system with dark/light theme, locale switching, and responsive layout.

## Features

- Multi-module system (preferences + layout)
- Dark/light/system theme with media query detection
- Locale selector with translated preview content
- Persistence plugin saves preferences to localStorage

## Run

```bash
pnpm install
pnpm dev
```
