# Dashboard Loader

A dashboard that loads multiple resources in parallel with per-resource status and retry.

## Features

- Parallel resource loading (profile, preferences, activity)
- Per-resource status cards with timing and attempt counts
- Combined progress bar and overall status derivation
- Configurable reload and error simulation

## Run

```bash
pnpm install
pnpm dev
```
