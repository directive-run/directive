# Time Machine

A drawing canvas with full time-travel debugging -- undo, redo, replay, and snapshot export/import.

## Features

- Canvas drawing with color picker and brush size
- Undo/redo via snapshot history
- Replay mode animates through all snapshots
- Export/import snapshots as JSON and changeset grouping

## Run

```bash
pnpm install
pnpm dev
```
