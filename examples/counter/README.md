# Counter

The simplest Directive example -- a counter with a constraint that clamps negative values to zero.

## Features

- Increment, decrement, and reset events
- `doubled` derivation computed from count
- Constraint auto-fires when count goes negative
- Built with `@directive-run/el` for lightweight DOM binding

## Run

```bash
pnpm install
pnpm dev
```
