# Form Wizard

A multi-step form with progress tracking, per-step validation, and a review step.

## Features

- Three-step wizard (credentials, profile, plan selection)
- Multi-module architecture (wizard + validation modules)
- Progress bar with step indicators
- Back/next navigation gated by validation constraints

## Run

```bash
pnpm install
pnpm dev
```
