# Async Chains

A multi-module system demonstrating chained async resolution across three steps: Auth, Permissions, and Dashboard.

## Features

- Three namespaced modules with cross-module dependencies
- Sequential async constraint chains
- Visual chain progress with per-step status
- Event timeline with full trace logging

## Run

```bash
pnpm install
pnpm dev
```
