# Notifications

A toast notification system with auto-dismiss, stacking limits, and priority levels.

## Features

- Multi-module system (notifications + app)
- Configurable max-visible toast stacking
- Auto-dismiss with constraint-driven timers
- Action log recording notification lifecycle events

## Run

```bash
pnpm install
pnpm dev
```
