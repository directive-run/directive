# AI Checkpoint

A multi-stage AI pipeline with save/restore checkpoints at each stage.

## Features

- Six-stage pipeline with progress tracking
- Save and restore checkpoints at any stage
- Auto-run mode to advance the pipeline automatically
- Event timeline showing stage transitions

## Run

```bash
pnpm install
pnpm dev
```
