# Dynamic Modules

Demonstrates registering and unregistering modules at runtime using `system.register()`.

## Features

- Starts with a single dashboard module
- Dynamically registers counter, weather, and dice modules on demand
- Per-namespace subscriptions via `subscribeModule`
- Modules can be removed and re-added at runtime

## Run

```bash
pnpm install
pnpm dev
```
