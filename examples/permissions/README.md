# Permissions

A role-based access control system with user switching, permission badges, and gated actions.

## Features

- Three user roles (admin, editor, viewer)
- Permission badge grid derived from current role
- Article list with role-gated edit/publish/delete actions
- Admin panel visible only to admin users

## Run

```bash
pnpm install
pnpm dev
```
