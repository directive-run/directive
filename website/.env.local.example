# Required for AI docs chatbot
OPENAI_API_KEY=sk-...        # Embeddings + RAG query
ANTHROPIC_API_KEY=sk-ant-...  # Chat response generation (Claude Haiku)
DISABLE_DAILY_CAP=true       # Bypass daily question cap (dev only)
